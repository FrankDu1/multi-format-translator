version: '3.8'

services:
  frontend:
    build: .
    image: translator-frontend:latest
    container_name: translator-frontend
    restart: unless-stopped
    ports:
      - "5001:5001"
    env_file:
      - .env
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
